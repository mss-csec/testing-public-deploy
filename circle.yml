general:
  branches:
    ignore:
      - deployed

test:
  post:
    - echo "Done testing"

deployment:
  production:
    branch: master 
    commands:
      - chmod +x ./post
      - chmod +x ./deploy
      - ./deploy
